#include "golem.h"
#include "pch.h"
#include "enemy.h"

using namespace std;
/*
void golem::golemfight(character *other)
{


	other->currentHealth -= damage;
	cout << name << " hit " << other->name << " for " << damage << " damage." << endl;
	cout << other->name << " is at " << other->currentHealth << "/" << other->totalHealth << " health." << endl;
}

void golem::golemsetStats(int level)
{


	if (damage < 1)
		damage = 1;

}
*/
