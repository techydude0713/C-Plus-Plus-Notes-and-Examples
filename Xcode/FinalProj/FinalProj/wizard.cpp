#include "wizard.h"
using namespace std;
/*
void wizard::wizfight(character *other)
{

    cout << "1. Fight" << endl;
    cout << ">";
    cin >> option;
    if (option == 1)
    {
        other->currentHealth -= damage;
        cout << name << " hit " << other->name << " for " << damage << " damage. " << endl;
        cout << other->name << " is at " << other->currentHealth << "/" << other->totalHealth << " health." << endl;
    }
    else if (option == 2)
    {
        other->currentHealth -= magdam;
        cout << name << " hit " << other->name << " for " << magdam << " damage. " << endl;
        cout << other->name << " is at " << other->currentHealth << "/" << other->totalHealth << " health." << endl;
		
	}
}


void wizard::wizsetStats(int level)
{
	int baseStat = 2 * level;
	int x = rand() % baseStat + 1;

	damage = x;
	currentHealth = totalHealth = x * 3;
}

bool wizard::wizisAlive()
{
    if (currentHealth > 0)
        return true;
    else
        return false;
}
*/
